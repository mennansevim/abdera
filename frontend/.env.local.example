# cp .env.local.example .env.local - .env.local commit'lenmez (Next.js varsayılanı)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Yalnızca herkese açık demo yayınında true yapılır. Normal kurulumda
# giriş alanları gerçek kullanıcı bilgileri için boş gelir.
NEXT_PUBLIC_DEMO_ENABLED=false
