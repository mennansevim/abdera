services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-abdera}
      POSTGRES_USER: ${POSTGRES_USER:-abdera}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD .env icinde tanimli olmali}
    ports:
      - "5432:5432"
    volumes:
      - abdera_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-abdera} -d ${POSTGRES_DB:-abdera}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      # docker-compose ag adi uzerinden baglanir - .env'deki POSTGRES_HOST=db ile tutarli.
      ConnectionStrings__Default: Host=${POSTGRES_HOST:-db};Port=${POSTGRES_PORT:-5432};Database=${POSTGRES_DB:-abdera};Username=${POSTGRES_USER:-abdera};Password=${POSTGRES_PASSWORD:?}
      Frontend__Origin: http://localhost:3000
      Auth__KeysDirectory: /app/keys
    volumes:
      # Data Protection anahtarlari - kalici olmazsa her restart'ta oturumlar dusuer.
      - abdera_dpkeys:/app/keys
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8080}
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_healthy

volumes:
  abdera_pgdata:
  abdera_dpkeys:
